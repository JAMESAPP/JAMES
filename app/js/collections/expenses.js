define([
	'backbone'
], function (Backbone){
	var Collection = Backbone.Collection.extend({
		// url: "app/data/categories.json",

		// initialize: function() {
		// 	this.fetch({async: false});
		// }
	});

	return Collection;
});
